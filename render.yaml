services:
  - type: web
    name: screenshot-to-code-backend
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: IS_PROD
        value: "true"